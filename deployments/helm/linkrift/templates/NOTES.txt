Linkrift has been deployed!

{{- if .Values.ingress.enabled }}

Access the application:
  App:      https://{{ .Values.ingress.hosts.app.host }}
  Redirect: https://{{ .Values.ingress.hosts.redirect.host }}
  API Docs: https://{{ .Values.ingress.hosts.app.host }}/api/docs
{{- else }}

Get the application URL by running:
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name=web,app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl port-forward $POD_NAME 3000:{{ .Values.web.port }}
  echo "Visit http://localhost:3000"
{{- end }}

Services deployed:
{{- if .Values.api.enabled }}
  - API ({{ .Values.api.replicaCount }} replicas)
{{- end }}
{{- if .Values.redirect.enabled }}
  - Redirect ({{ .Values.redirect.replicaCount }} replicas)
{{- end }}
{{- if .Values.worker.enabled }}
  - Worker ({{ .Values.worker.replicaCount }} replicas)
{{- end }}
{{- if .Values.web.enabled }}
  - Web ({{ .Values.web.replicaCount }} replicas)
{{- end }}

To check deployment status:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }}
